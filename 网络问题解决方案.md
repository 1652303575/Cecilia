# DeepSeek 连接问题解决方案

## 问题现象

生成邮件或运行 `python check_models.py` 时，可能出现超时、连接失败、鉴权失败或限流错误。

应用使用以下服务端点：

```env
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
ANTHROPIC_MODEL=deepseek-v4-pro
```

## 首先检查配置

确认项目根目录的 `.env` 包含有效的新 Token：

```env
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
ANTHROPIC_AUTH_TOKEN=你的新DeepSeekToken
ANTHROPIC_MODEL=deepseek-v4-pro
HOST=127.0.0.1
PORT=8000
SESSION_SECRET=请替换为随机长字符串
```

注意：

- 不要使用已经出现在聊天、日志或截图里的 Token。
- 模型标识使用 DeepSeek 官方支持的值，例如 `deepseek-v4-pro`。
- 修改 `.env` 后必须重启应用。

## 诊断步骤

### 1. 验证模型连接

```bash
pip install -r requirements.txt
python check_models.py
```

若验证失败，错误信息可以区分 Token、限流和网络问题。

### 2. 检查端点能否访问

```bash
curl -I https://api.deepseek.com
```

若服务器无法建立 HTTPS 连接，请检查出站网络、防火墙、DNS 或公司网络策略。

### 3. 配置代理（仅在网络环境需要时）

编辑 `.env` 添加代理地址：

```env
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
```

将端口替换为本机代理工具实际使用的 HTTP 代理端口，然后重新运行连接验证并重启应用。

## 错误处理

| 错误表现 | 处理方式 |
|----------|----------|
| 缺少 `ANTHROPIC_AUTH_TOKEN` | 创建 `.env` 并填入新的 Token |
| 密钥无效或未配置 | 在 DeepSeek 平台重建 Token 并替换旧值 |
| 请求过于频繁 | 等待后重试，并检查账户配额 |
| 网络连接失败或超时 | 检查网络、防火墙及可选代理设置 |
| 模型不可用 | 将 `ANTHROPIC_MODEL` 改为官方支持的模型标识 |

## 生产环境排查

对于 systemd 部署，在服务器上执行：

```bash
cd /opt/trade-email-generator
source venv/bin/activate
python check_models.py
journalctl -u trade-email -n 100 --no-pager
```

确认 `/opt/trade-email-generator/.env` 只允许必要账户读取，且没有把 Token 打印到日志中。
